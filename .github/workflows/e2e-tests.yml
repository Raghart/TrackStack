name: E2E Tests Minimal

on:
  push:
    branches: 
      - main

jobs:
  playwright_tests:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Say Hello
        run: |
          echo "Hello World!"
