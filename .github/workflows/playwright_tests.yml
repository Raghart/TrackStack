name: Playwright Minimal Tests

on: 
  push:
    branches:
      - main

jobs:
  playwright-tests:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v4
    
    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
  
    - name: Hello World debug
      run: echo "✅ Workflow se está ejecutando correctamente"
